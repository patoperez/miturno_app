.DS_Store
Thumbs.db

# Secretos y entorno — NUNCA se commitean (el repo es público)
.env
.env.*

# Supabase CLI (estado local de la máquina)
supabase/.temp/
supabase/.branches/

# Node
node_modules/

# Capturas de pantalla para revision: pueden contener datos reales.
# El repo es PUBLICO, asi que NUNCA se commitean.
capturas/
